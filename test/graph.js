require('./vertex.js');

var expect = require('chai').expect;

describe('Graph', function() {

	describe('.Vertex', function() {
		it('should return the Vertex constructor.', function() {
			//expect(Graph).itself.to.have.property('Vertex', Vertex);
		});
	});

	describe('constructor()', function() {
		it('should return a new instance of graph.');
		it('should contain exactly two vertices');
	});

	describe('split()', function() {
		it('should return a new instance of Node.');
	});

});